{
  "name": "studiob-home-post-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Studio B Home — luxury Instagram post designer (suppliers, preview, export)",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "echo \"App scaffold coming next — run a static server on . or add Vite\"",
    "test": "echo \"No tests yet\" && exit 0"
  }
}
